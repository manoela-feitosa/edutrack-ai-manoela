# Change: create-subjects-table

## Why
We need a subjects table to store school subject data for the application.

## What Changes
Create the `subjects` table with the following fields:
- id
- name
- teacher
- hours
- user_id

## Impact
Allows the system to persist subject records linked to a user.